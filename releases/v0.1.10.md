## 管理端 v0.1.10

### 变更

- feat(admin): 视频选片先压缩，保存时再上传云存储
- feat(admin): 视频压缩预览与直传 API，修复 FFmpeg 转码稳定性
- feat(api): 新增视频压缩预览与延迟上传契约
- feat(admin): 媒体冗余扫描全库集合并二次核实引用
- feat(admin): 媒体冗余弹窗展示可删除与需确认状态
- feat(migrate): 迁移失败时清空旧环境媒体引用并规范文档 ID
- fix(build): macOS Intel 打包跳过 libswiftCore 并补 Swift RPATH

### 安装包

| 平台 | 产物 |
|------|------|
| macOS Apple Silicon | `.dmg`、`.app.tar.gz` |
| macOS Intel | `.dmg`、`.app.tar.gz` |
| Windows x64 | NSIS `.exe`、`.msi` |

