## 管理端 v0.1.6

### 变更

- fix(admin): 移除 geolocation 未使用的 corelocation prelude 导入
- fix(ci): macOS arm64 改用 macos-15 以兼容 corelocation-rs Swift 桥接
- refactor(admin): sync-center 精简环境迁移完成反馈
- chore(build): Windows cargo 补充 FFMPEG_DIR 与 LIBCLANG_PATH

### 安装包

| 平台 | 产物 |
|------|------|
| macOS Apple Silicon | `.dmg`、`.app.tar.gz` |
| macOS Intel | `.dmg`、`.app.tar.gz` |
| Windows x64 | NSIS `.exe`、`.msi` |

### 自动更新

- release 版启动后从 GitHub Release 检查更新（latest.json）
- 详见 `admin/README.md`

