## 管理端 v0.1.4

### 变更

- fix(build): 修复 release 脚本模板字符串语法错误
- fix(admin): macOS 改用 CoreLocation 原生定位并补充 Info.plist 权限说明
- refactor(admin): 自动更新改走 GitHub Release 替代 CloudBase COS

### 安装包

| 平台 | 产物 |
|------|------|
| macOS Apple Silicon | `.dmg`、`.app.tar.gz` |
| macOS Intel | `.dmg`、`.app.tar.gz` |
| Windows x64 | NSIS `.exe`、`.msi` |

### 自动更新

- release 版启动后从 GitHub Release 检查更新（latest.json）
- 详见 `admin/README.md`

