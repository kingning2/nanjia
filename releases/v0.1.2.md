## 管理端 v0.1.2

### 变更

- chore(admin): 移除 adapters 空目录占位
- chore(admin): 补充 Windows 桌面开发与 FFmpeg 脚本
- feat(admin): 优化图片列表与内容表单布局
- fix(admin): 新增 macOS 平台配置与 Info.plist 位置权限
- fix(admin): 修复 macOS Finder 启动时因日志目录依赖 cwd 崩溃

### 安装包

| 平台 | 产物 |
|------|------|
| macOS Apple Silicon | `.dmg`、`.app.tar.gz` |
| macOS Intel | `.dmg`、`.app.tar.gz` |
| Windows x64 | NSIS `.exe`、`.msi` |

### 自动更新

- release 版启动后从 CloudBase COS 检查更新（v0.1.4 及以上）
- 详见 `admin/README.md`

