## 管理端 v0.1.7

### 变更

- fix(ci): 限制 LIBCLANG_PATH 仅 Windows target，修复 macOS CI ffmpeg-sys-next 构建
- fix(build): FFMPEG_DIR 回顶层 cargo env，修复 Windows 本地 `pnpm tauri build`

### 安装包

| 平台 | 产物 |
|------|------|
| macOS Apple Silicon | `.dmg`、`.app.tar.gz` |
| macOS Intel | `.dmg`、`.app.tar.gz` |
| Windows x64 | NSIS `.exe`、`.msi` |

### 自动更新

- release 版启动后从 GitHub Release 检查更新（latest.json）
- 详见 `admin/README.md`
