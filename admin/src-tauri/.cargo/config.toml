[env]
PATH = { value = "C:\\Program Files\\Git\\bin", force = false }
# build script 不读 [target.*.env]，Windows 本地 pnpm tauri build 需顶层 FFMPEG_DIR
FFMPEG_DIR = "D:\\ffmpeg-dev\\ffmpeg-n8.1-latest-win64-gpl-shared-8.1"

# ponytail: LIBCLANG_PATH 仅 Windows target；顶层会污染 macOS CI bindgen
[target.x86_64-pc-windows-msvc.env]
LIBCLANG_PATH = "C:\\Program Files\\LLVM\\bin"
